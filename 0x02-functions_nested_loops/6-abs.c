#include "main.h"
/**
 * _abs - computes absolute value
 *
 * Description: computes abs
 *
 * @n: int being checked
 *
 * Return: the abs of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
